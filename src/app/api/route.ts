// This is a placeholder file. You can delete it if you don't need a root API route.
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'AlgoTrade Navigator API' });
}
