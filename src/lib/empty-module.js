// Stub module — replaces native Node packages (e.g. @mapbox/node-pre-gyp)
// that Turbopack cannot bundle. These are server-side only and are
// externalized via serverExternalPackages in next.config.ts.
module.exports = {};
